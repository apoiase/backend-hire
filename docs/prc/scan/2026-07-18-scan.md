# Scan código → realidade — backend-hire (2026-07-18)

Fontes: `graphify-out/graph.json` (claims), `.understand-anything/knowledge-graph.json`,
Steampipe/aws (profile `sa_automation_ro`), gitleaks (árvore + histórico completo).
Legenda: ✅ confirmado · ⚠️ divergência · ❓ validar · ❌ não encontrado

## Compute (serverless.yml)

Não há `serverless.yml`, `package.json` nem qualquer manifesto de deploy no
repo. O README cita a stack esperada (Node.js, GraphQL, REST, MongoDB, Lambda,
AWS) apenas como **referência para o candidato** — nenhuma dessas tecnologias
está de fato implementada aqui.

| Código afirma | Realidade | Status |
|---|---|---|
| n/a — sem manifesto de deploy | n/a | — |

## Filas e eventos

Nenhuma fila, tópico ou evento referenciado no repo.

## Bancos (nosso escopo — verificado)

README cita a collection `users` como especificação do desafio (id,
firstName, lastName, dateBirth) — não é uma collection real em produção,
é o esquema mínimo que o candidato deve implementar no próprio fork.

## Config e secrets

- `.gitignore` prevê `.env`/`.env.test` (padrão Node), mas não há `.env.example`
  nem qualquer arquivo de config no repo.
- Gitleaks (árvore + histórico completo, 2 commits): **0 findings**.

## Terceiros (validar — sem acesso)

Nenhum terceiro referenciado no código (não há código de produto).

## Realidade AWS (Steampipe, profile `sa_automation_ro`)

Busca por recursos com `backend-hire`/`backend_hire` no nome:

| Recurso | Resultado |
|---|---|
| `aws_lambda_function` | ❌ nenhum resultado |
| `aws_s3_bucket` | ❌ nenhum resultado |
| `aws_secretsmanager_secret` | ❌ nenhum resultado |

**0 recursos AWS confirmados** com o nome do repo — esperado: `backend-hire`
é um template de teste de contratação, forkado pelos candidatos (o código
de fato nunca é implantado sob este nome).

## Divergências e ações

Nenhuma divergência. Repo é um enunciado de desafio (README + LICENSE +
.gitignore), sem código de produto, sem infra própria. Nada a reconciliar.
